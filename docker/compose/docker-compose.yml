version: '3.8'

services:
  universodomedo:
    container_name: dev-udm
    build: .
    environtment:
      NODE_ENV: production
    networks:
      wireguard:
        ipv4_address: 10.1.1.115
networks:
  wireguards:
    external:true
