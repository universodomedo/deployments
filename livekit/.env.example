# Generate a real key/secret pair with:
#   docker run --rm livekit/livekit-server generate-keys
#
# Format: "<api-key>: <api-secret>". Multiple pairs can be comma-separated.
LIVEKIT_KEYS=APIxxxxxxxxxxxx: your-generated-secret-here
